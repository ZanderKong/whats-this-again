{
  "name": "whats-this-again",
  "version": "0.5.2",
  "private": false,
  "description": "Chrome extension for contextual AI explanations and reusable local reading memories.",
  "type": "module",
  "scripts": {
    "validate": "node tools/validate-extension.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test",
    "demo:annotations": "node tools/serve-fixtures.mjs",
    "package": "node tools/package-extension.mjs"
  },
  "keywords": [
    "chrome-extension",
    "manifest-v3",
    "ai",
    "reading",
    "openai-compatible"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
